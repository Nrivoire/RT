#ifndef PP_H
# define PP_H

# define CELSHADING 60
# define PIXEL_SIZE 12
# define PIXEL_SIZE_SQRD 148
# define BLUR_RADIUS 3

# define MIN(i,j) ((i) < (j) ? (i) : (j))
# define MAX(i,j) ((i) < (j) ? (j) : (i))

/*
** --post_process--
*/


#endif
